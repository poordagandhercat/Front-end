import React from 'react';
import { Input } from 'antd';

export default class ListIpt extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function

  constructor() {
    super();
    this.state = {

    };
  }

  render () {
    const {  } = this.state
    return (
      <Input />
    )
  }
}
